{
  "compilerOptions": {
    "target": "ES6", // Use ES6 for modern JS features.
    "module": "NodeNext", // Align with the CommonJS module system.
    "outDir": "./dist", // Output compiled files to a `dist` directory.
    "rootDir": "./src" // Specify `src` as the root directory for source files.
  },
  "include": ["src"], // Include all files in the `src` folder.
  "exclude": ["node_modules", "dist"] // Exclude `node_modules` and `dist` folders.
}
